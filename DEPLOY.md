# LOS CHATETAS - FÚTBOL EDITION

## Deployment Configuration

### Build Command
```bash
cd client && npm install && npm run build && cd ../server && npm install
```

### Start Command  
```bash
cd server && npm start
```

### Environment Variables
- `PORT`: 10000
- `CLIENT_URL`: https://los-chatetas.onrender.com
- `NODE_ENV`: production
